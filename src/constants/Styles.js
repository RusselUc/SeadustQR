import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        paddingVertical: 15,
        paddingLeft: 15,
    },

    image: {
        width: 50,
        height: 50,
        marginRight: 10
    },

    icon: {
        color: '#000',
        marginLeft:'auto',
        fontSize: 20,
        marginRight: 20
    }
})

export default styles